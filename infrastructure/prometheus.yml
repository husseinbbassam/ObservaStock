global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'observastock-dev'
    replica: '0'

scrape_configs:
  # Scrape metrics from OpenTelemetry Collector
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']
        labels:
          service: 'otel-collector'
  
  # Scrape Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'prometheus'

  # If services expose Prometheus metrics directly, add them here
  # - job_name: 'trading-api'
  #   static_configs:
  #     - targets: ['host.docker.internal:5000']
  #       labels:
  #         service: 'trading-api'
  
  # - job_name: 'price-service'
  #   static_configs:
  #     - targets: ['host.docker.internal:5001']
  #       labels:
  #         service: 'price-service'
